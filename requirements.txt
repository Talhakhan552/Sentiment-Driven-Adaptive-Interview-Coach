# Core speech & audio
SpeechRecognition>=3.10.0
librosa>=0.10.0
soundfile>=0.12.1
pyaudio>=0.2.14

# Scientific computing
numpy>=1.24.0

# Already bundled with Python
# json, warnings, os, random — no install needed
